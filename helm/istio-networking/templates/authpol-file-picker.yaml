apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: jwt-verification-policy
  namespace: default
spec:
  selector:
    matchLabels:
      app: auth
  action: ALLOW
  rules:
  - from:
    - source:
        requestPrincipals: ["*"]